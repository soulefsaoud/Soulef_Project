{% extends 'base.html.twig' %}

{% block title %}Fitness{% endblock %}

{% block body %}
    <h1>Fitness</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fitness.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fitness.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ fitness.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{fitness.createdAt | date('d-m-Y') }}</td>
            </tr>
            <tr>
                <th>VideoUrl</th>
                  <td><iframe width="560" height="315" src="https://www.youtube.com/embed/{{ fitness.videoUrl }}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                <td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fitness_index') }}">reenir à la liste</a>

    <a href="{{ path('fitness_edit', {'id': fitness.id}) }}">modifier</a>

    {{ include('admin/fitness/_delete_form.html.twig') }}
{% endblock %}
